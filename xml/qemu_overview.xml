<?xml version="1.0" encoding="utf-8"?>
<?xml-stylesheet href="urn:x-suse:xslt:profiling:docbook51-profile.xsl"
    type="text/xml"
    title="Profiling step"
?>
<!DOCTYPE chapter
[
   <!ENTITY % entities SYSTEM "entity-decl.ent">
   %entities;
]>
<chapter xmlns="http://docbook.org/ns/docbook" xmlns:xi="http://www.w3.org/2001/XInclude" xmlns:xlink="http://www.w3.org/1999/xlink" version="5.1" xml:id="cha-qemu-overview">
 <title>&qemu; Overview</title>
 <info/>
 <para>
  &qemu; is a fast, cross-platform Open Source machine emulator which can
  emulate a huge number of hardware architectures for you. &qemu; lets you run
  a complete unmodified operating system (&vmguest;) on top of your existing
  system (&vmhost;).
 </para>
 <para>
  You can also use &qemu; for debugging purposes - you can easily stop your
  running virtual machine, inspect its state and save and restore it later.
 </para>
 <para>
  &qemu; consists of the following parts:
 </para>
 <itemizedlist mark="bullet" spacing="normal">
  <listitem>
   <para>
    processor emulator (x86, s390x, PowerPC, Sparc ...)
   </para>
  </listitem>
  <listitem>
   <para>
    emulated devices (graphic card, network card, hard drives, mice ...)
   </para>
  </listitem>
  <listitem>
   <para>
    generic devices used to connect the emulated devices to the related host
    devices
   </para>
  </listitem>
  <listitem>
   <para>
    descriptions of the emulated machines (PC, Power Mac ...)
   </para>
  </listitem>
  <listitem>
   <para>
    debugger
   </para>
  </listitem>
  <listitem>
   <para>
    user interface used to interact with the emulator
   </para>
  </listitem>
 </itemizedlist>
 <para>
  As a virtualization solution, &qemu; can be run together with the &kvm;
  kernel module. If the &vmguest; hardware architecture is the same as
  &vmhost;'s architecture, &qemu; can take advantage of the &kvm; acceleration.
 </para>
</chapter>
